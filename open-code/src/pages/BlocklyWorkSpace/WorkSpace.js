import React from 'react';
import {WorkspaceStyles as useStyles} from "./styles";
function WorkSpace(props) {
    const classes = useStyles();
    return (
        <div>This is workspace</div>
    );
}

export default WorkSpace;